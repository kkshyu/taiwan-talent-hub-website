# 社群場地申請部署驗證

查核日期：2026-09-06（台灣時間）。對外流程依 [07 對外口徑母檔](../../00_專案治理與營運/母檔/07_對外口徑母檔.md)。

## 發布版本

| 項目 | 已確認結果 |
|---|---|
| 正式入口 | https://www.emoji.tw/event-application |
| 英文／日文 | https://www.emoji.tw/en/event-application ／ https://www.emoji.tw/ja/event-application |
| 管理入口 | https://www.emoji.tw/admin#applications |
| GitHub | `twouring/emoji-website`，`main` |
| 版本 | `8fa5ee24f6cfa25a511f6f7989e36a363627f661` |
| Zeabur 服務 | `emoji-website`，`6a43d7bed525b066f248b4a6` |
| 環境 | `6a43a7afe33d94ef307b23a5` |
| 部署 | `6a9c6a917066abe5dab30154`，最終狀態 `RUNNING` |
| 建置完成 | 2026-09-05 19:19:04 UTC（台灣 2026-09-06 03:19:04） |

GitHub 推送後未立即出現新部署，因此對既有服務執行重新部署。Zeabur 取得上述版本；部署後再次查核遠端 `main`，仍為相同 SHA。

收尾時另一項並行工作將遠端 `main` 更新為 `09af34126275c1ae017f45201bfbfbbb7002bfb5`（父版本為本次 `8fa5ee2`）。當時 Zeabur 仍為本次版本 `8fa5ee2`、狀態 `RUNNING`；該後續資安版本不屬於本次部署驗證範圍。

本次以獨立發布 checkout 提交 16 個活動申請相關檔案，未帶入原工作區其他資安、效能及前台修改。本次未操作原工作區 HEAD 或 index，保留其他工作的修改及提交。此部署驗證及母檔同步紀錄另存於本機，未加入上述程式發布 commit。

## 正式環境驗證

- 正式容器四個核心檔案的 SHA-256 與隔離發布版完全一致，見下表。
- `/api/health` 回應 `{ "ok": true, "db": true, "dbConfigured": true }`。
- 使用既有管理權限唯讀查詢 `/api/admin/event-applications`：200、`Cache-Control: no-store`，查核時共 0 筆。確認申請資料表可正常查詢；未輸出金鑰或聯絡資料。
- 未登入的 `POST /api/event-applications`、`GET /api/me/event-applications`、`GET /api/admin/event-applications` 均回應 401。
- `/auth/google` 正確回應 302 至 `accounts.google.com`；本次未在正式站重做 Google 授權及登入完成流程。
- Chrome 實際渲染三語申請頁，均為 200、語言正確、未登入時禁止送出。390px 手機與 1440px 桌面沒有水平溢出。
- 三語活動、社群合作、空間介紹共九個頁面，渲染後皆有正確語系的申請入口；頁面無 JavaScript 執行錯誤。
- 首次瀏覽器檢查遇導流頁導覽逾時；以 60 秒導覽上限重驗後，全數通過，未攔截或替換外部資源。

| 正式容器檔案 | SHA-256 |
|---|---|
| `server.js` | `8a2d58aa703b0d5ddb3615e6a5785fa4729478d27ea2a439152a0b2b1a2b8553` |
| `lib/event-applications.js` | `e1c23fa9b6569070b5283909a1b4021ef32b65245123901b38d01d15165a55a1` |
| `public/event-application.html` | `b56c0f86e9bdf974db62c0728bf01eac4f9f94f8f663a8a73da7f4df12835867` |
| `public/admin.html` | `95af9e5e2095e3b74f181363e4eca8dad8d41a19549d6fb2c85fa62487672cbe` |

## 發布前驗證與界線

隔離發布版完整測試為 **220 通過、0 失敗、0 略過**，其中申請功能 11 項資料庫／輸入檢查及 4 項前台檢查均通過；後端語法與 `git diff --check` 通過。送件、權限隔離、重送、審核衝突及申請人查看回覆已於隔離 PostgreSQL／Chrome 環境實測。

本次未在正式環境新增測試申請或進行審核寫入，亦未自動寄信、收費或發布活動。母檔、營運計畫及網站 README 已同步正式部署狀態；母檔一致性檢查結果為 0 筆殘留。
